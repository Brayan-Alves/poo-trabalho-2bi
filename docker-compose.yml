services:
  mysql:
    image: mysql:8.0
    container_name: mysql-streaming
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: streaming_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: